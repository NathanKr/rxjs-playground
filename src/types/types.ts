// Define the type for the emitted value (number and sender information)
export type NumberAndSender = [number, chrome.runtime.MessageSender]; // Adjust the sender type as per your sender information
